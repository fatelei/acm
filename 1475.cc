#include <iostream>
#include <cstdio>
#include <string>
using namespace std;

void decode(string packet) {
    string length = 
}

int main() {
    int cases;
    string packet;
    while (cases--) {
        getline(cin, packet);
        decode(packet);
    }
    return 0;
}
